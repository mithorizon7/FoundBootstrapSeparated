# Database Configuration (Required)
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/startup_bootcamp

# Session Security (Required)
# Generate a secure random string for session encryption
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-random-string-here

# Application Environment
# Set to 'production' for production deployments
NODE_ENV=development

# Optional: Replit-specific variables (automatically set on Replit)
# REPL_ID=your-repl-id
# REPLIT_DOMAINS=your-repl-domains